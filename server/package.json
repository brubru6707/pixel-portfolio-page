{
  "name": "pixel-portfolio-presence-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal WebSocket presence server: broadcasts player positions so others render as non-interactive ghosts.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
